version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - prealloc
  settings:
    errcheck:
      check-type-assertions: true
    govet:
      enable:
        - shadow

formatters:
  enable:
    - gofmt
    - goimports

